package com.softgraf.farmacia.util;

//import javax.persistence.EntityManager;
//import javax.persistence.EntityManagerFactory;
//import javax.persistence.Persistence;

//classe substituída pelo EntityManagerProducer do CDI (Context and Dependency Injection)
//classe utilitária, a qual acessa o persistence.xml para fazer a conexão com o banco de dados.
public class JpaUtil {

/*	
	//contexto static -> factory, não gera objeto.
	private static EntityManagerFactory factory;
	
	static {
		factory = Persistence.createEntityManagerFactory("farmaciaPU");
		
	}
	
	public static EntityManager getEntityManager() {
		return factory.createEntityManager();
	}
	
	public static void close() {
		factory.close();
	}
*/

}
